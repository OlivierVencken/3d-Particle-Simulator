package com.particle.sim.ui.theme;

/** Colors for one semantic component variant and its interaction states. */
public record ComponentPalette(
        Color background,
        Color hovered,
        Color active,
        Color border,
        Color text) {
}
